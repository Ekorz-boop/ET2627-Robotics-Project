# Robotics_Project
The files for the robotics project. Use UV to get dependencies etc.
The client notebook can be run in the jupyter environment on each jetbot. The server is simply run on a computer on the same network. Make sure to config all ip adresses and bot colors etc.
